# Environment variables example file
# Copy this to .env and fill in your values

PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=appdb

# API Configuration (update this after starting ngrok for testing)
REACT_APP_API_URL=http://localhost:5000

JWT_SECRET=your_super_secret_jwt_key_here_change_this
JWT_EXPIRY=15m

# Email Configuration (Gmail SMTP)
# To use Gmail, you need to create an App Password:
# 1. Go to Google Account > Security > 2-Step Verification (enable if not already)
# 2. Go to App passwords (at the bottom of 2-Step Verification page)
# 3. Select "Mail" and "Windows Computer" (or any device)
# 4. Generate and copy the 16-character password
# 5. Use that password below (without spaces)

EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-char-app-password
EMAIL_FROM_NAME=Trnsferio
