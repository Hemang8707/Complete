import React, { useState } from "react";
import { useNavigate } from "react-router-dom";

function SignIn() {
  const [formData, setFormData] = useState({ 
    dealerName: "",
    dealerCode: "", 
    password: "" 
  });
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState("");

  const navigate = useNavigate();

  const handleChange = (e) => {
    setFormData({ ...formData, [e.target.name]: e.target.value });
    if (error) setError("");
  };

  const handleSubmit = async (e) => {
    e.preventDefault();
    setLoading(true);
    setError("");

    if (!formData.dealerName || !formData.dealerCode || !formData.password) {
      setError("Please enter dealer name, dealer code, and password");
      setLoading(false);
      return;
    }

    try {
      const response = await fetch("http://localhost:5000/api/login", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          dealerName: formData.dealerName.trim(),
          dealerCode: formData.dealerCode.trim(),
          password: formData.password,
        }),
      });

      const data = await response.json();

      if (data.success) {
        localStorage.setItem('isAuthenticated', 'true');
        localStorage.setItem('userData', JSON.stringify(data.user));
        console.log("✅ Login successful:", data.user);
        navigate("/form");
      } else {
        setError(data.error || "Login failed. Please try again.");
      }
    } catch (error) {
      console.error("❌ Login error:", error);
      setError("Connection error. Please check your internet connection and try again.");
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="signin-container">
      {/* Left Hero Section */}
      <div className="hero-section">
        <div className="hero-content">
          <h1 className="hero-title">
            Giving you the <span className="highlight">power to design</span> innovative card products your customers want.
          </h1>
          <p className="hero-description">
            Step into the world of smarter card issuance. Whether you're a digital bank, fintech or enterprise, our platform gives you the flexibility, control and insights to create products your customers love, without the complexity.
          </p>
          <div className="hero-buttons">
            <button className="btn-primary">Talk to us →</button>
            <button className="btn-secondary">Create your card →</button>
          </div>
        </div>
        
        {/* Card Stack Illustration */}
        <div className="card-stack">
          <div className="card card-1"></div>
          <div className="card card-2"></div>
          <div className="card card-3">
            <div className="card-chip"></div>
            <div className="card-dots">•••• 1309</div>
          </div>
        </div>
      </div>

      {/* Right Sign In Section */}
      <div className="signin-box">
        <h2 className="signin-title">
          SIGN IN /{" "}
          <span
            style={{ color: "blue", cursor: "pointer" }}
            onClick={() => navigate("/Signup")}
          >
            SIGN UP
          </span>
        </h2>

        {error && (
          <div className="error-message">
            ⚠️ {error}
          </div>
        )}

        <form onSubmit={handleSubmit} className="signin-form">
          <input
            type="text"
            name="dealerName"
            placeholder="👤 Dealer Name (e.g., SB Enterprises)"
            value={formData.dealerName}
            onChange={handleChange}
            disabled={loading}
            required
          />
          <input
            type="text"
            name="dealerCode"
            placeholder="🏢 Dealer Code (e.g., D001)"
            value={formData.dealerCode}
            onChange={handleChange}
            disabled={loading}
            required
          />
          <input
            type="password"
            name="password"
            placeholder="🔒 Password"
            value={formData.password}
            onChange={handleChange}
            disabled={loading}
            required
          />
          <button type="submit" disabled={loading}>
            {loading ? "Signing In..." : "Sign In"}
          </button>
        </form>
      </div>

      <style>{`
        @keyframes float {
          0%, 100% { transform: translateY(0px); }
          50% { transform: translateY(-8px); }
        }
        
        @keyframes slideIn {
          0% { 
            transform: translateY(50px) scale(0.9);
            opacity: 0;
          }
          100% { 
            transform: translateY(0) scale(1);
            opacity: 1;
          }
        }

        @keyframes slideInLeft {
          0% { 
            transform: translateX(-100px);
            opacity: 0;
          }
          100% { 
            transform: translateX(0);
            opacity: 1;
          }
        }

        @keyframes cardFloat {
          0%, 100% { transform: translateY(0) rotateY(0deg); }
          50% { transform: translateY(-10px) rotateY(5deg); }
        }
        
        @keyframes pulse {
          0%, 100% { transform: scale(1); }
          50% { transform: scale(1.05); }
        }

        @keyframes shake {
          0%, 100% { transform: translateX(0); }
          25% { transform: translateX(-5px); }
          75% { transform: translateX(5px); }
        }

        @keyframes shimmer {
          0% { transform: translateX(-100%); }
          100% { transform: translateX(100%); }
        }

        * {
          margin: 0;
          padding: 0;
          box-sizing: border-box;
        }

        .signin-container {
          display: flex;
          height: 100vh;
          background: linear-gradient(135deg, #f8f9ff 0%, #ffffff 100%);
          font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
          overflow: hidden;
        }

        /* Hero Section Styles */
        .hero-section {
          flex: 1;
          padding: 80px 60px;
          display: flex;
          flex-direction: column;
          justify-content: center;
          position: relative;
          background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
          animation: slideInLeft 1s ease-out;
        }

        .hero-content {
          max-width: 600px;
          z-index: 2;
        }

        .hero-title {
          font-size: 48px;
          font-weight: 800;
          color: white;
          line-height: 1.2;
          margin-bottom: 30px;
          animation: slideIn 0.8s ease-out;
        }

        .hero-title .highlight {
          color: #ffd700;
        }

        .hero-description {
          font-size: 18px;
          color: rgba(255, 255, 255, 0.95);
          line-height: 1.6;
          margin-bottom: 40px;
          animation: slideIn 1s ease-out;
        }

        .hero-buttons {
          display: flex;
          gap: 20px;
          animation: slideIn 1.2s ease-out;
        }

        .btn-primary, .btn-secondary {
          padding: 16px 32px;
          font-size: 16px;
          font-weight: 600;
          border: 2px solid white;
          border-radius: 50px;
          cursor: pointer;
          transition: all 0.3s ease;
        }

        .btn-primary {
          background: white;
          color: #667eea;
        }

        .btn-primary:hover {
          transform: translateY(-3px);
          box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .btn-secondary {
          background: transparent;
          color: white;
        }

        .btn-secondary:hover {
          background: rgba(255, 255, 255, 0.1);
          transform: translateY(-3px);
        }

        /* Card Stack Styles */
        .card-stack {
          position: absolute;
          right: 80px;
          top: 50%;
          transform: translateY(-50%);
          width: 400px;
          height: 250px;
          perspective: 1000px;
        }

        .card {
          position: absolute;
          width: 300px;
          height: 200px;
          border-radius: 20px;
          box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
          transition: all 0.4s ease;
        }

        .card-1 {
          background: linear-gradient(135deg, #b8c5ff 0%, #c9d5ff 100%);
          top: 0;
          left: -40px;
          transform: rotate(-10deg);
          animation: cardFloat 6s ease-in-out infinite;
          z-index: 1;
        }

        .card-2 {
          background: linear-gradient(135deg, #8aa5ff 0%, #a8bbff 100%);
          top: 20px;
          left: -20px;
          transform: rotate(-5deg);
          animation: cardFloat 5s ease-in-out infinite;
          animation-delay: 0.5s;
          z-index: 2;
        }

        .card-3 {
          background: linear-gradient(135deg, #4169ff 0%, #6087ff 100%);
          top: 40px;
          left: 0;
          transform: rotate(0deg);
          animation: cardFloat 4s ease-in-out infinite;
          animation-delay: 1s;
          z-index: 3;
          display: flex;
          flex-direction: column;
          justify-content: space-between;
          padding: 30px;
        }

        .card-chip {
          width: 50px;
          height: 40px;
          background: linear-gradient(135deg, #ffd700 0%, #ffed4e 100%);
          border-radius: 8px;
          align-self: flex-start;
        }

        .card-dots {
          color: white;
          font-size: 24px;
          font-weight: 600;
          letter-spacing: 4px;
          align-self: flex-end;
        }

        /* Sign In Box Styles */
        .signin-box {
          background: rgba(255, 255, 255, 0.98);
          backdrop-filter: blur(20px);
          padding: 60px 50px;
          width: 480px;
          display: flex;
          flex-direction: column;
          justify-content: center;
          box-shadow: -10px 0 40px rgba(0, 0, 0, 0.1);
          animation: slideIn 0.8s ease-out;
          position: relative;
          z-index: 10;
        }
        
        .signin-title {
          font-size: 32px;
          font-weight: 700;
          margin-bottom: 40px;
          background: linear-gradient(45deg, #667eea, #764ba2);
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          background-clip: text;
          animation: float 3s ease-in-out infinite;
          text-align: center;
        }

        .error-message {
          background: linear-gradient(45deg, #ff6b6b, #ee5a52);
          color: white;
          padding: 12px 20px;
          border-radius: 12px;
          margin-bottom: 20px;
          font-weight: 500;
          box-shadow: 0 4px 15px rgba(238, 90, 82, 0.3);
          animation: shake 0.5s ease-in-out;
          text-align: center;
        }
        
        .signin-form {
          display: flex;
          flex-direction: column;
          gap: 20px;
        }
        
        .signin-form input {
          padding: 18px 24px;
          border: 2px solid #e0e7ff;
          border-radius: 12px;
          font-size: 16px;
          transition: all 0.3s ease;
          outline: none;
        }

        .signin-form input:disabled {
          opacity: 0.7;
          cursor: not-allowed;
        }
        
        .signin-form input:not(:disabled):hover {
          border-color: #667eea;
          transform: translateY(-2px);
          box-shadow: 0 8px 20px rgba(102, 126, 234, 0.2);
        }
        
        .signin-form input:not(:disabled):focus {
          border-color: #667eea;
          box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }
        
        .signin-form input::placeholder {
          color: #9ca3af;
        }
        
        .signin-form button {
          background: linear-gradient(45deg, #667eea, #764ba2);
          color: white;
          padding: 18px;
          border: none;
          border-radius: 12px;
          cursor: pointer;
          font-size: 18px;
          font-weight: 600;
          transition: all 0.3s ease;
          position: relative;
          overflow: hidden;
          box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
          margin-top: 10px;
        }

        .signin-form button:disabled {
          opacity: 0.7;
          cursor: not-allowed;
          transform: none;
        }
        
        .signin-form button::before {
          content: '';
          position: absolute;
          top: 0;
          left: -100%;
          width: 100%;
          height: 100%;
          background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
          transition: left 0.5s;
        }
        
        .signin-form button:not(:disabled):hover {
          transform: translateY(-3px);
          box-shadow: 0 12px 35px rgba(102, 126, 234, 0.5);
        }
        
        .signin-form button:not(:disabled):hover::before {
          left: 100%;
        }

        /* Responsive Design */
        @media (max-width: 1024px) {
          .signin-container {
            flex-direction: column;
          }

          .hero-section {
            padding: 40px 30px;
            min-height: 50vh;
          }

          .card-stack {
            display: none;
          }

          .hero-title {
            font-size: 36px;
          }

          .signin-box {
            width: 100%;
            padding: 40px 30px;
          }
        }

        @media (max-width: 768px) {
          .hero-title {
            font-size: 28px;
          }

          .hero-description {
            font-size: 16px;
          }

          .hero-buttons {
            flex-direction: column;
          }

          .btn-primary, .btn-secondary {
            width: 100%;
          }

          .signin-title {
            font-size: 24px;
          }
        }
      `}</style>
    </div>
  );
}

export default SignIn;